<%= form_for @pipe, :html => { :class => "form-horizontal pipe" } do |f| %>

    <% if @pipe.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@pipe.errors.count, "error") %> prohibited this pipe from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @pipe.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :in, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :in, :class => 'form-control' %>
    </div>
    <%= error_span(@pipe[:in]) %>
  </div>
  <div class="control-group">
    <%= f.label :out, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :out, :class => 'form-control' %>
    </div>
    <%= error_span(@pipe[:out]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            pipes_path, :class => 'btn btn-default' %>

<% end %>
